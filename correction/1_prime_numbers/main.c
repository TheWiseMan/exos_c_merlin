int main() {
    int N = 100;
    int liste[] = {0};

    int i = 0;
    int X = 2;
    while (i<N) {
        for (/* tous elts k dans liste*/) {
            // X est divisible par liste[k] ?
            // si oui, X est pas premier
        }
        // sinon X est premier
        // on ajoute X à la liste
        // i++
        X++;
    }
}